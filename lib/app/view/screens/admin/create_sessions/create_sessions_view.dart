import 'package:flutter/material.dart';

class CreateSessionsView extends StatelessWidget {
  const CreateSessionsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
